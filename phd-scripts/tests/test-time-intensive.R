library(testthat)
context("time-intensive")

library(simeta)
library(neet)

test_that("simulation workds on 10 trials", {
  # sim <- metasim(trials = 10)
  #
  # expect_neet(sim)
})

test_that("simulation workds on 100 trials", {
  # sim <- metasim(trials = 100)
  #
  # expect_neet(sim)
})

test_that("simulation workds on 1000 trials", {
  # sim <- metasim(trials = 1000)
  #
  # expect_neet(sim)
})
